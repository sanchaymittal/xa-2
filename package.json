{
  "name": "ax-abstraction",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/contracts",
    "packages/fusion-server"
  ],
  "scripts": {
    "lint:all": "yarn workspaces foreach --parallel run lint",
    "test:all": "yarn workspaces foreach --parallel  run test",
    "clean:all": "yarn workspaces foreach --parallel run clean",
    "build:all": "yarn workspaces foreach --parallel -pt run build",
    "tsp:root": "tsp",
    "tsp": "tsp",
    "version": "yarn version"
  },
  "packageManager": "yarn@3.4.1"
}
